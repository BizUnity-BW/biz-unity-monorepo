# Same Supabase project as the tenant app: an admin is a UserProfile with
# systemRole = SYSTEM_ADMIN. Anon key only — the service-role key must never
# reach a browser bundle.
VITE_SUPABASE_URL=https://tjakgmwvuzdpycigufmo.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_URL=http://localhost:4000
